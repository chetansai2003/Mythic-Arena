# Step 5 — cinematic UX

Implement page 7 of the six-part plan in JavaScript/JSX. Keep the existing authoritative engine and online lifecycle unchanged.

1. Add an optional lazy React Three Fiber/Drei arena using original geometry. Keep SVG while loading, in Low/reduced-motion mode, or after WebGL failure. Bound resolution, parallax, particles and hidden-tab work.
2. Add GSAP portal/crest and card-reveal sequences; Motion UI transitions; accepted-event-only summon/impact effects and a victory crest. Cinematics remain decorative, skippable, and independent of readiness/turn deadlines.
3. Finish first-match guidance, tap/keyboard card inspection, graphics/mute controls, real screen states and obsolete copy. Never cover tactical controls with effects.
4. Test no-3D, slow loading, WebGL failure, reduced motion, event deduplication, keyboard/touch journeys and all three viewports. Measure optional scene bytes and browser frame timing on the available device. Record actual results, limitations, asset licenses and the event map.

React Three Fiber 9.7 declares React >=19 <19.3. Align React/ReactDOM to the current 19.2 patch rather than bypassing peer dependency checks. No server rules or release infrastructure changes are planned.
